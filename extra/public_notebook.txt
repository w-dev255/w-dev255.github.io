#JavaScript Agent
A JavaScript agent must have at least four components:

Parser: interprets the typed string.

Dispatcher: decides which function to execute.

Internal state: variables, memory, context.

Output generator: produces text, boxes, responses.
